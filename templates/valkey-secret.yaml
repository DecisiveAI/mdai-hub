apiVersion: v1
kind: Secret
metadata:
  name: valkey-secret
  labels:
    app: valkey
  annotations:
    "helm.sh/hook": pre-install
type: Opaque

data:
  VALKEY_PASSWORD: {{ randAlphaNum 32 | b64enc | quote }}
  VALKEY_URI: {{ include "valkey.valkeyURI" . | b64enc }}
